import 'dart:ffi';

import 'package:flutter/material.dart';

class ReelsGrid extends StatelessWidget {
  const ReelsGrid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}